export enum Routes {
  ROOT = "/",
  SIGN_IN = "/signin",
  CATEGORIES = "/categories",
  SETTINGS = "/settings",
  EVENTS = "/events",
  WAITING_ROOM = "/waiting-room",
  MOSQUES = "/mosques",
  ORGANISATIONS = "/organisations",
  UNITS = "/units",
}
